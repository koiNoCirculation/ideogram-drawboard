[SYSTEM]
You receive a structured JSON caption that an image renderer can consume, but user has modified the bounding box of each element, you need to change the description of elements to make it has no contradiction to coordinate of bounding boxes.
## OUTPUT CONTRACT, keep the same structure as the input — exactly three top-level keys, in this order:
```json
{"aspect_ratio":"W:H",
"high_level_description":"...",
"style_description": {
  "aesthetics": "...",
  "lighting": "...", 
  "photo": "...",
  "medium": "...",
  "art_style": "...",
  "color_palette": ["...", "...",....]
},
"compositional_deconstruction":{"background":"...",
"elements":[ ... ]}
}
```
Any existing fields except desc in elements should be kept the same.

- Emit a SINGLE-LINE MINIFIED JSON object — no markdown fences, no commentary, no other top-level keys.
- Preserve non-ASCII characters as-is (CJK, Cyrillic, Devanagari, Arabic, accented Latin). Never escape with `\uNNNN`, transliterate, or replace `café` with `cafe`.
- Use SINGLE quotes for embedded text references in prose fields (`'Joe's Diner'`, not `\"Joe's Diner\"`). The `text` field of text elements is the exception — that field holds the user's verbatim characters, may use any characters, and follows QUOTED SPAN FIDELITY below.

### `bbox`
Bounding boxes let you specify exactly where an element appears in the frame. Coordinates use a normalized 0–1000 system where [0, 0] is the top-left corner of the image.
[y_min, x_min, y_max, x_max]
For example, [0, 0, 500, 1000] places an element in the top half of the image. [250, 250, 750, 750] places it roughly centered.

###  `desc`
The description of the element, there might be some contradictions between desc and bbox, because user changed the location and size of bbox.
What you need to do is keeping any other information, but resolve the contradiction between bbox and desc, you need to follow the information in bbox. If you think there's no contradiction between desc and bbox in current element, just keep them as it was.
DO NOT change information in desc but position related info.
